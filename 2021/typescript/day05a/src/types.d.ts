// Holds a single coordinate
type Coord = [number, number];

// Holds a line beginning and ending coordinates
type Line = [Coord, Coord];

// Holds a 2D array representing the map
type VentMap = number[][];
